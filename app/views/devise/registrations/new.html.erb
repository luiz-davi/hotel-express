<div class="login-page" style="background-image: url(<%= image_url("/images/background.png") %>)">
  <div class="card registration" style="border-radius: 10px;">
    <h2 class="login-title">Cadastre-se</h2>

    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      
      <div class="field">
        <label for="user_email">Email</label>
        <%= f.email_field :email, autofocus: true, class: "form-control", placeholder: "@gmail.com", autocomplete: "email" %>
      </div>
      <p></p>

      <div class="field">
        <label for="user_password">Senha</label>
        <% if @minimum_password_length %>
          <em>(No mínimo <%= @minimum_password_length %> números.)</em>
        <% end %><br />
        <%= f.password_field :password, class: "form-control", placeholder: "******", autocomplete: "new-password" %>
      </div>
      <p></p>

      <div class="field">
        <label for="user_password_confirmation">Confirmação de senha</label>
        <%= f.password_field :password_confirmation, class: "form-control", placeholder: "******" , autocomplete: "new-password" %>
      </div>
      <br>

      <div class="actions">
        <%= f.submit "Cadastrar", class: "login-button" %>
      </div>
      <%= render "devise/shared/links" %>
      <%= render "devise/shared/error_messages", resource: resource %>
    <% end %>
  </div>
</div>